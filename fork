To create a new child process using fork system call.
#include <stdio.h>
#include <sys/types.h>;
#include <unistd.h>;
int main()
{
	if (fork() == 0)
		printf("INSIDE TRUE BLOCK\n");

	else
		printf("INSIDE ELSE BLOCK\n");
}
